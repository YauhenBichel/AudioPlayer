// Release-only settings. AdMob identifiers are public (they ship inside every
// app binary), so they belong here rather than in a secret — but they are the
// owner's to fill in. Left empty, the app shows Google's test ads, which is
// the only thing a development build should ever show.
export const ADMOB_BANNER_UNIT_ID = '';
